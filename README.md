The First file contains a python program that perform Basic mathematical Operations on two numbers by taking the input values from the user

The second file contains a python program that takes the user's first nam and last name as inputs  and concatenatesthe nameswith a personalized greeting message

The third file contain a Python program that takes an integer input from the user and check if it even or odd

The fourth file contains a python program that displays the sum of the integers from 1 to 50 
